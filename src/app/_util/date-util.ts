// import { Regex } from '../_constant/regex';
// import { Locale } from './../_constant/locale';

// export class DateUtil {

//     public static fromDateString(date: string, locale: Locale): Date;
//     public static fromDateString(date: string, format: string): Date;
//     public static fromDateString(date: string, a: Locale | string): Date {
//         switch (locale) {
//             case Locale.IT:
//                 if (Regex.DD_MM_YY.test(date?.trim())) {
//                     throw new Error('Invalid date: ' + date + ', ' + locale);
//                 }
//                 break;

//             default:
//                 break;
//         }
//     }

//     public static toDays(date: Date): number {

//     }

//     private replaceSlash(date: string): string {
//         return date.replace
//     }

// }
